[project]
name = "cdaa-integration-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.28",
    "pytest==9.0.2",
    "pytest-httpserver==1.1.5",
]

[tool.pytest.ini_options]
testpaths = ["."]
